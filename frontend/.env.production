# Production environment variables for Wails build
# In production, the frontend communicates with the backend via the same origin
# The API_BASE_URL will be determined dynamically in the api.tsx file
